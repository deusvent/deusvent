namespace logic {

};

interface Timestamp {
  [Name = now]
  constructor();
};

interface ServerTimestamp {
  
};

interface SyncedTimestamp {
  constructor();
  void adjust([ByRef]ServerTimestamp server_time, [ByRef]Timestamp sent_at, [ByRef]Timestamp received_at);
  Timestamp now();
};
